import {useMemo,useState} from 'react';import {HiChevronLeft,HiChevronRight} from 'react-icons/hi2';import {useTasks} from '../context/TaskContext';
const names=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
export default function Calendar(){const{tasks}=useTasks();const[date,setDate]=useState(new Date());const year=date.getFullYear(),month=date.getMonth();const first=new Date(year,month,1).getDay(),days=new Date(year,month+1,0).getDate();const cells=useMemo(()=>Array.from({length:first+days},(_,i)=>i<first?null:i-first+1),[first,days]);const change=n=>setDate(new Date(year,month+n,1));const toISO=d=>`${year}-${String(month+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;return <div className="space-y-6 animate-fade-up"><div><h1 className="text-3xl font-bold">Calendar</h1><p className="mt-1 text-slate-500">See your deadlines at a glance.</p></div><div className="card overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5 dark:border-slate-700"><button className="btn-secondary !px-3" onClick={()=>change(-1)}><HiChevronLeft/></button><h2 className="text-lg font-bold">{date.toLocaleDateString(undefined,{month:'long',year:'numeric'})}</h2><button className="btn-secondary !px-3" onClick={()=>change(1)}><HiChevronRight/></button></div><div className="grid grid-cols-7 border-l border-slate-100 dark:border-slate-700">{names.map(n=><div key={n} className="border-b border-r border-slate-100 py-3 text-center text-xs font-bold uppercase text-slate-400 dark:border-slate-700">{n}</div>)}{cells.map((day,i)=><div key={i} className="min-h-24 border-b border-r border-slate-100 p-2 dark:border-slate-700 sm:min-h-32">{day&&<><span className={`grid h-7 w-7 place-items-center rounded-full text-xs font-semibold ${toISO(day)===new Date().toISOString().slice(0,10)?'bg-indigo-600 text-white':''}`}>{day}</span><div className="mt-1 space-y-1">{tasks.filter(t=>t.dueDate===toISO(day)).slice(0,2).map(t=><div title={t.title} key={t.id} className={`truncate rounded px-1.5 py-1 text-[10px] font-semibold ${t.completed?'bg-slate-100 text-slate-400 line-through dark:bg-slate-700':'bg-indigo-100 text-indigo-700 dark:bg-indigo-500/20 dark:text-indigo-200'}`}>{t.title}</div>)}</div></>}</div>)}</div></div></div>}
