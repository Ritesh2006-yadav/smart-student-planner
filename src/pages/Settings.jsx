import {useTheme} from '../context/ThemeContext';import {useToast} from '../context/ToastContext';import {useTasks} from '../context/TaskContext';
export default function Settings(){const{dark,toggleTheme}=useTheme();const{toast}=useToast();const{tasks}=useTasks();return <div className="mx-auto max-w-3xl space-y-6 animate-fade-up"><div><h1 className="text-3xl font-bold">Settings</h1><p className="mt-1 text-slate-500">Tune the planner around your routine.</p></div><div className="card divide-y divide-slate-100 dark:divide-slate-700"><div className="flex items-center justify-between gap-5 p-6"><div><h2 className="font-bold">Dark mode</h2><p className="mt-1 text-sm text-slate-500">Use a darker, easier-on-the-eyes appearance.</p></div><button aria-label="Toggle dark mode" onClick={toggleTheme} className={`relative h-7 w-12 rounded-full transition ${dark?'bg-indigo-600':'bg-slate-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${dark?'left-6':'left-1'}`}/></button></div><div className="p-6"><h2 className="font-bold">Data & privacy</h2><p className="mt-1 text-sm text-slate-500">Your {tasks.length} tasks and all notes live only in this browser’s local storage.</p><button onClick={()=>toast('Your data is stored locally on this device.','info')} className="btn-secondary mt-4">Learn more</button></div></div></div>}
